# 云游戏 MVP（1080p60）验收清单与优化要点

## 1. MVP 范围（必须交付）
- 分辨率：**1080p**（1920×1080）。
- 帧率：目标 **60fps**（可接受短时波动，但不可长时间降到 45fps 以下）。
- 场景：同城/同运营商优先，支持公网 P2P（必要时可走 TURN）。
- 输入：鼠标/键盘可玩（MVP 可先保证鼠标与键盘；手柄后续迭代）。
- 稳定性：连续运行 ≥ 30 分钟不崩溃、不黑屏、无明显音画不同步。

## 2. P0 体验指标（可量化）
> 建议用“显示串流统计信息”面板 + 录屏对比验收。

- **平均帧率**：≥ 58fps；1 分钟内 1% low 不低于 50fps。
- **首帧时间（TTFF）**：从点击连接到首帧显示 ≤ 3s（同城网络）。
- **冻结/卡顿**：连续冻结（画面不更新）单次 < 200ms；每 10 分钟累计冻结 < 1s。
- **端到端延迟（主观）**：鼠标移动/点击到画面反馈明显可感延迟不超过“可玩阈值”（同城目标 ≤ 50ms）。
- **弱网降级**：在 1% 丢包或 20ms jitter 下，画面可降码率但不应频繁断连/长时间黑屏。

## 3. 必测用例（按优先级）
- **P0-连接**：多次冷启动连接（≥ 20 次），成功率 ≥ 95%；失败必须有明确错误提示与可重试。
- **P0-输入**：锁定光标后快速甩鼠标 + 连点（10s），无明显“拖影式迟滞”；键盘按键无丢失。
- **P0-网络切换**：Wi‑Fi ↔ 有线/热点切换一次，允许短暂停顿但应可恢复或给出重连按钮。
- **P1-长稳**：60 分钟压力跑（高负载游戏/3D 负载），无内存持续增长导致崩溃。

## 4. 现有方案可复用（你们已经具备）
- WebRTC P2P 串流与信令转发（连接、offer/answer/candidate、DataChannel 输入）。
- 串流统计面板（用于 fps/丢包/RTT/jitter/冻结观察）。
- 节点侧能力可扩展（Windows 守护程序 + 强制重启用于资源回收）。

## 5. 现阶段必须补齐/优化（避免走弯路）
- **低延迟输入通道**：鼠标移动等高频输入走“不可靠 DataChannel”，点击/键盘等关键输入走“可靠通道”。
- **建连速度**：去掉 ICE candidate 的人为延迟，缩短首帧与重连时间。
- **码率策略**：避免把 `x-google-min/max/start-bitrate` 全锁死为同值；改为“目标码率 + 下限”，让弱网可自适应降码率而不是冻结。
- **输入协议一致性**：确保可靠/不可靠通道发送的二进制格式一致（避免解析不一致导致异常输入）。

